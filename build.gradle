plugins {
	id 'java'
}

allprojects {
	group = 'kr.flab'
	version = '0.0.1-SNAPSHOT'

	repositories {
		mavenCentral()
	}

	tasks.withType(Test) {
		useJUnitPlatform()
	}
}

tasks.withType(Test) {
	useJUnitPlatform()
}

tasks.getByName('jar') {
	enabled = false
}